{
  "name": "hardcover-proxy",
  "version": "1.0.0",
  "description": "Backend + proxy for Hardcover app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd ../frontend && npm install -g pnpm && pnpm install && pnpm run build",
    "build-and-serve": "cd ../frontend && npm run build && cd ../backend && node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
